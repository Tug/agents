package epfl.lia.logist.security;

/**
 * Enumeration of possible operators
 */
public enum SecurityRuleOpEnum {
	SROP_EQ,	// the thread group must be equal to ref
	SROP_NEQ,	// the thread group must not be equal to ref
	SROP_ANY	// the thread group can be any
}
