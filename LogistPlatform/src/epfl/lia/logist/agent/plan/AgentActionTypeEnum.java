package epfl.lia.logist.agent.plan;

public enum AgentActionTypeEnum {
	
	/* represents a 'Move' action */
	MOVE,
	
	/* represents a 'Pickup' action */
	PICKUP,
	
	/* represents a 'Deliver' action */
	DELIVER,
}
