package epfl.lia.logist.tools;

/**
 * This class contains various constants describing fixed parameters
 * of the platform. These params are only updated after major revisions.
 * No parameter of the simulation should be specified here, because it
 * is preferable to allow configurable values
 *
 * @author malves
 * @since 23.06.07
 * @version 1.0.67
 */
public class LogistConstants {

	/**
	 * Represents the major version of the platform. This value
	 * is only updated after major changes, like rewriting the
	 * platform from scratch.
	 */
	public static final int VERSION_MAJOR = 1;
	
	/**
	 * Represents the minor version of the platform. This value
	 * is only updated after minor changes, like partial rewrites,
	 * major bug tracking.
	 */
	public static final int VERSION_MINOR = 0;
	
	/**
	 * Represents the current revision of the platform. This value
	 * is used when developping, after each commit.
	 */
	public static final int VERSION_REVISION = 67;
}
